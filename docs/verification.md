# 動作検証の記録

最終検証日: 2026-09-21。開発環境はmacOS / Apple Silicon、Go 1.27.1、Wails 2.15.0、Node.js 24.2.0、ripgrep 15.2.0です。

## 実行単位の履歴と結果一覧

- 実行ボタンから終了・停止までを1つの実行として記録し、結果確認の上部で切り替える。最新への追従、過去の選択の維持、切り替え中に別の実行の詳細を表示しないことを確認した。従来の実行IDのない履歴はファイル単位の表示を維持する。
- 選択した実行の対象は完了・変更不要でも通常一覧へ残し、それ以前の対象外の完了・変更不要だけを「完了済み」へ分類する。進捗・状態別件数も当該実行の対象だけを数える。以前の完了ファイルを再び対象にした場合、通常一覧へ戻る。
- 設定・ルール・対象・結果・個別試行の証跡を実行ごとに保全する。再試行・変更破棄・同じ試行IDでの再開後も過去の結果を保持し、累積差分は最初の処理前から選択した実行時点までを表示する。修正由来の実行IDでルールの色を判定し、同一実行内の複数試行は同じ色にする。
- 過去の実行は閲覧専用とし、その時点のルール表示とレポート出力を確認した。過去の「実行中」表示から現在の処理を停止できないこと、現在のロック・処理状態に従って操作を制限することも確認した。
- 準備失敗・停止・アプリ再起動時の中断を記録する。保存済み復帰情報から使用量を回収し、通信結果不明の扱いを保持する。実行結果の保存失敗時は、保全が完了するまで再試行・設定変更などを進めない。累積本文は不変Gitコミットから必要なファイルだけを読み、元リポジトリが失われた場合は読取エラーを表示する。
- `npm test`（TypeScript、Node 106件、全internal/CLIのGoテスト、Go vet）が成功。macOS arm64・Windows amd64のGUI・CLIをビルドした。アプリ起動・画面操作・実LLMへの送信は行っていない。

## 最新・過去の修正のルールID色分け

- 対応状況、差分・ソース表示、詳細ヘッダーのルールIDを、最新の修正は紫、過去の修正は青灰色で表示。凡例とホバー説明を付け、同一変更ブロックに両方がある場合もIDを二重表示しない。
- 累積・個別詳細に由来の試行IDを付与し、保存済み履歴を変えずに判定。無変更・失敗の再試行後は以前の採用修正を過去色とし、未採用項目や由来不明の項目には最新色を付けない。ソース表示では同じルールIDでも行の修正時期が変われば再表示する。
- `npm test`（TypeScript、Node 88件、全internal/CLIのGoテスト、Go vet）が成功。macOS arm64・Windows amd64のGUI・CLIをビルドした。アプリ起動・画面操作は行っていない。

## 再試行後の完了状態と累積結果

- 再試行が追加修正なしでも、以前の採用済み変更が残るファイルは「完了」とする。試行履歴は変更不要のまま保持し、新しいコミットを作らない。初回変更なし・破棄後・差分相殺は変更不要、失敗・要確認はそれぞれの状態を保つ。
- 対応状況と変更内容の初期表示を「全体（開始前 → 現在）」へ変更。Gitの不変コミットから初期・最新の採用済み本文を取得し、失敗した提案や検証中の作業コピーを混ぜない。個別履歴には当時の対応状況・差分を残す。
- 複数試行の採用済み対応を集約し、最初の変更前・現在の変更後へ行番号を対応付ける。追加修正なしでも以前の修正を保持し、部分的な取り消し・変更破棄・古い未解決項目を除外する。重複行などで位置が曖昧な場合は説明を保持し、ルールの行マーカーを推測しない。自由文の位置は「修正時の位置」として区別する。
- `npm test`（TypeScript、Node 84件、全internal/CLIのGoテスト、Go vet）が成功。最後の座標境界・表示位置の追加修正も対象Goテスト13件、previewテスト11件で確認。macOS arm64・Windows amd64のGUI・CLIをビルドした。実LLMへの送信、アプリ起動・画面操作、ユーザーのプロジェクト変更は行っていない。

## 完了済みの開閉ラベルを境界へ配置

- 通常ファイル・開閉ラベル・完了済みファイルの順に配置。境界が表示範囲より下にある間だけラベルを下端に留め、同じ1本のスクロールで境界を通過できる。全件完了の場合はラベルを先頭に表示する。
- ラベルの34pxを仮想リストの高さ・選択行の位置計算に含め、キーボードで選んだ行が下端のラベルに隠れないようにした。折りたたみ時は現在のスクロール位置を可能な範囲で保つ。
- 関連Nodeテスト27件とTypeScriptチェックが成功。macOS arm64・Windows amd64のGUI・CLIをビルドした。アプリ起動・画面操作は行っていない。

## 一覧スクロールの統一と差分ルールIDの重複解消

- 共通のファイル一覧を1つの仮想リスト・スクロール領域へ変更。「完了済み（件数）」の開閉バーは通常ファイルと完了済みファイルの境界に配置し、境界が表示範囲より下にある間だけ下端に留める。展開した完了済みファイルはバーの直後へ追加する。全件完了済みの場合はバーを先頭に置き、未完了の空欄は表示しない。
- 完了済みに`done`（完了）と`skipped`（変更不要）を含めた。両方ともチェックボックス・選択件数・一括選択から外し、戻すアイコンで再試行に追加する。結果の変更不要フィルターでは折りたたみを開き、該当ファイルを表示する。
- 統合差分は連続する削除・追加行でルールIDを重複排除し、記録のある最初の位置に1回表示する。離れた箇所・別hunk・追加側だけにある別ルールの表示、末尾改行なしのマーカー、変更前／変更後の単独表示を回帰テストで確認した。ルール名称のホバーと右詳細を開く操作は保持する。
- `npm test`（TypeScript、Node 76件、全internal/CLIのGoテスト、Go vet）が成功。macOS arm64・Windows amd64のGUI・CLI・同梱rgをビルドした。実LLMへの送信、アプリ起動・画面操作、Windows実機確認は行っていない。

## 未修正・再試行の表示と完了済み一覧

- 実行設定と結果確認に共通のファイル一覧を導入。ファイル名を強調した相対パスと右端の状態を短い行で表示し、再試行・処理中・未修正・失敗・要確認・変更不要の順に整列する。完了済みは最下部の独立した領域へ移し、初期状態を折りたたみにした。
- 完了済みのチェックボックスを戻すアイコンに置換。操作前のファイル選択を保存したうえで、対象を選択済みの再試行として一覧上部へ戻す。完了済みは選択件数・一括選択から除き、結果画面の完了フィルターから再試行に追加した際はフィルターを解除する。
- 内部状態は既存の`pending`と`resumeRequested`で表現し、新しい状態は増やさない。完了から再試行への変更、再抽出、アプリ再起動、次の処理開始までを一時Gitリポジトリで検証。選択・修正済み内容・コミット・履歴を保持し、処理開始時だけ再試行フラグを解除することを確認した。
- フロント側では状態順、再試行と未修正の別集計、完了済みの初期折りたたみとチェックボックス非表示、戻すアイコン、10,000件の仮想表示、結果の詳細・差分・履歴の維持を検証した。
- `npm test`（TypeScript、Node 71件、全internal/CLIのGoテスト、Go vet）が成功。macOS arm64・Windows amd64のGUI・CLI・同梱rgをビルドした。実LLMへの送信、アプリ起動・画面操作、Windows実機確認は行っていない。

## 共通ルールの該当なし・完了までの継続

- 独立レビューは共通ルールにも理由付き`not_applicable`を許可する。必ず全ルールを評価し、修正済み・既に適合している場合は`satisfied`を使う。評価漏れ・重複・空の理由・未知のルール・判定の矛盾は引き続き拒否する。パーサー、3プロバイダーの模擬HTTP、採用判定を確認した。
- 計画登録後の`needs_human`は、`blocked`のルールと理由、または`blocked`の項目と`holdReason`が必要。作業途中・再検証待ち・残り予算の推測による早期終了を同じ会話へ差し戻す。具体的な保留は独立レビューへ進まず維持し、計画自体を作れない場合の事前保留も維持する。
- 未設定の検証・レビュー・外側の試行回数、ターン数、処理時間を0＝上限なしへ変更。暗黙の3回・12ターン・600秒を廃止。明示した正の上限・料金上限・停止操作は維持する。出力トークンとファイルサイズの既定値は維持し、既存の正の設定値は書き換えない。
- ターン上限なしの場合、累計32ツール・512 KiB読取の打ち切りも行わない。1応答8ツール、ツール応答・要求・対象ファイルのサイズ境界は保持する。模擬HTTPによる39 editor要求・38ツール・4候補検証、累計読取512 KiB超からの完了、50ターン以降のレビュー、5回目の外側試行での成功、手動キャンセル、明示した時間・回数上限を確認した。
- 実行設定の左側ファイル一覧から候補ルールIDを削除。本文ヘッダーのIDと右側ルール詳細は維持。3つの上限欄を「未設定（上限なし）」表示に変更し、出力トークンとファイルサイズの標準値表示は維持する。
- `npm test`（TypeScript・Node 62件・全internal/CLIのGoテスト・Go vet）と、追加の完了継続テストが成功。macOS arm64・Windows amd64のGUI・CLI・同梱rgをビルドした。実LLMへの送信、アプリ起動や画面操作、Windows実機確認は行っていない。

## 実行設定・確認のファイルブラウザーと行別ルール表示

- 実行設定・確認を対象フォルダ画面と共通の一覧／本文／ルール詳細の構成へ変更。ファイル行と本文ヘッダーの右端に状態、ファイル名の直後に候補ルールを表示。対象チェックは実行設定で行い、確認は確定した選択を読み取り専用で表示する。
- 未選択・接続不足・設定エラー・実行中などで無効になる次へ・前へ・タブ・実行には、同じ判定から禁止理由の吹き出しを表示。内容とルールは、選択操作が無効でも閲覧できる。
- 本文は次回処理する入力を読む。作業コピーがある場合の採用済み内容、元フォルダの内容、失敗した候補を区別する。ワークスペース・対象フォルダ・キュー所属・パスとリンクを検証し、読み取りによってキューやGitを変更しないことを確認した。
- 処理上限・料金は最下部のアコーディオンへ移動。新規・デモは未設定（0）で保存し、実行時だけ上限の標準値を補完する。保存・再起動・再実行で未設定値が維持され、自動再試行も有限の標準値で制限されることを確認した。
- CSV取り込みをAPI・保存モデル・パーサー・UI・生成済みバインディングから削除。追加ボタン右の「…」からパッケージをインポート／書き出しできる。上書き・マージ、ID衝突の接尾辞、大文字小文字・ID長・補助資材・編集ロック・検証失敗時の保持をテストした。
- 対応内容が空の項目を対応状況の表示と件数から除外。新しい候補では編集ごとの計画項目IDを必須にし、未指定・null・空配列は機械検証に進む前に診断を返して同じ会話で修正できる。実際の置換から変更前後の行範囲を記録し、対応状況と差分のルールIDに反映する。共通アンカー、挿入・削除・複数編集・不一致の拒否・候補との対応・旧記録の保持をテストした。
- `npm test`（TypeScript・Node・全internal/CLIのGoテスト・Go vet）と、追加した実行入力プレビューのテストが成功。macOS arm64・Windows amd64のGUI・CLI・同梱rgをビルドした。画面操作・アプリ起動・実LLMへの送信・Windows実機確認は行っていない。

## 対象フォルダのファイル閲覧

- フォルダ選択欄の下へ、ファイル名を強調した相対パス一覧と行番号付き本文を追加。検索・更新・幅調整・キーボード選択・左右独立のスクロールに対応し、ファイル一覧は表示範囲の近くのみ描画する。
- 元の対象フォルダを読み取り専用で表示。ルール・キュー・処理対象の選択・修正後の作業コピーとは独立し、Gitの管理ファイルと未追跡ファイルを列挙する。ignore対象の未追跡ファイル・Git内部ファイル・リンク・特殊ファイルは除外する。
- Goの対象テストで、階層・サブフォルダ・ignore・未コミット内容・元ファイルの参照・パス逸脱とリンクの拒否・ワークスペース不一致・サイズと形式の制限・一覧上限・Git未導入時の診断を確認。閲覧によってソースやキューを書き換えないことを検証した。
- フロントエンドのテストで、HTMLを文字として表示すること、改行と行番号、20,000行の表示上限の明示、プレビューで処理対象や結果を変更しないことを確認。切り替え前の応答を無視する制御とスクロールのレイアウトはコードレビューを実施した。
- `npm test`が成功（TypeScript、Node 52件、全internal/CLIのGoテスト、Go vet）。macOS arm64・Windows amd64のGUI・CLI・同梱rgをビルドした。画面操作・アプリ起動・実LLMへの送信・Windows実機確認は行っていない。

## キュー・実行結果の保存先を内部管理

- 実行設定の保存先入力欄・選択ボタンと、保存先フォルダの選択処理を削除。LLM接続欄は1列で表示する。結果レポートを書き出す際のファイル選択ダイアログは維持する。
- 新規ワークスペースは内部の`workspaces/<ID>/runs/`を使用。通常の設定保存で渡された`queuePath`は無視し、そのワークスペースの保存先を維持する。記録済みの保存先や履歴の移動・削除は行わない。
- 対象テストで、保存先の上書きや空指定の無視、他の設定項目の保存、既存の独自保存先と履歴の切替・再起動後の維持を確認。新規作成・複製・対象フォルダ変更・デモの保存先分離と、CLI初期設定の指定も確認した。
- `npm test`が成功（TypeScript、Node 48件、全internal/CLIのGoテスト、Go vet）。macOS arm64・Windows amd64のGUI・CLI・同梱rgをビルドした。実ユーザーのデータ変更、アプリ起動・画面操作・実LLMへの送信・Windows実機確認は行っていない。

## 結果詳細からの変更破棄

- 「再試行に追加」の右へ赤字の「変更破棄」を追加。確認ダイアログに対象ファイルと復元先を示す。採用済み変更が残っていれば、最新の試行が変更不要でも操作できる。実行中・閲覧専用・採用済み変更なしでは無効にする。
- 専用作業コピーの対象ファイルだけを初期コミットの内容へ戻し、取り消しのコミットを追加する。他ファイル、元の対象フォルダ、過去の試行・成果物・使用量は保持する。破棄の記録は試行回数に加えず履歴へ表示し、過去の試行を開いたときは変更破棄前の記録であることを明示する。
- 完了後は待機中へ戻す。破棄以前の計画・候補・既読ルール・不合格差分は次の実行へ引き継がず、通算料金と使用量不明フラグは保持する。明示的な再実行と同じ扱いで再投入し、費用上限がある場合の使用量不明による停止は維持する。
- 実際の一時Gitリポジトリを使い、2ファイルの分離、変更済みから変更不要を経た破棄、再抽出・再起動・再実行、準備記録・復元・ステージ・コミット・保存途中からの復旧を検証。手動編集・未知のコミット・インデックスだけの変更・シンボリックリンク・閲覧専用の場合に変更しないことも確認した。
- 変更破棄の対象テストと`npm test`が成功（TypeScript、Node 48件、全internal/CLIのGoテスト、Go vet）。macOS arm64・Windows amd64のGUI・CLI・同梱rgをビルドした。実ユーザーの変更破棄、アプリ起動・画面操作・実LLMへの送信・Windows実機確認は行っていない。

## 処理上限・料金をワークスペースへ分離

- 「処理上限・料金の設定」をルールから実行設定タブへ移動。展開時は画面をスクロールでき、対象ファイル一覧の高さを確保する。9項目を各ワークスペースの`setting.json`内の`config`に保存する。
- `.oborules`はversion 3とし、設定は対象glob・除外glob・検証コマンドの3項目のみ。上限・単価9項目を含む設定や旧versionは読み込まない。インポート・エクスポート・複製・切替・再起動で、既存のワークスペース設定が保持されることをテストした。
- デモの20ターン・16,384出力トークンは新規ワークスペースの初期値へ移動。既存ワークスペースへのデモルール導入では、上限・単価を維持する。
- ファイルサイズ上限の変更は対象の再抽出を要求し、その他の上限・単価変更では対象ファイルの選択を失わないことを確認。実行ごとのターン判定と通算料金の制限は変更していない。
- `npm test`が成功（TypeScript、Node 45件、全internal/CLIのGoテスト、Go vet）。macOS arm64・Windows amd64のGUI・CLI・同梱rgをビルドした。実LLMへの送信・アプリ起動・画面操作・Windows実機確認は行っていない。

## 修正箇所ごとのリスクと対応状況

- 計画に`risk`・`holdReason`を記録し、箇所・ルール・対応内容・期待条件とともに試行の`changes`へ保存する。保留ルールにも具体的な保留箇所を記録できるが、候補検証・採用の保留条件は維持する。
- 「修正済み」はコミット・元ファイル・候補・計画・機械検証・独立レビューが一致する採用済みの項目だけに付く。未採用の案は「未反映」、メインの保留箇所は「要確認」に分ける。独立レビューのルール単位の保留と指摘箇所は、その粒度で別行に記録し、計画項目へ推測で対応付けない。
- 「対応状況」タブの行を開くとリスク・期待する結果・判断理由を表示する。選択した過去の試行の記録を使い、ライブ更新で手動のタブ選択を変えない。記録のないリスク・当時のルール名は補完せず、ルール参照先が現在の定義であることを明示する。
- JSONレポートに対応状況を含め、ルール変更後も当時の名称・説明を保持する。旧履歴の状態ファイルは、選択ファイルの詳細表示と出力時にだけ読み込み、起動時の全件読み込みを避ける。
- ローカル模擬HTTPと実際の検証・コミット処理を通し、処理中の記録、最終状態、JSONL再読込、JSON出力、コミット後の中断からの復帰を確認。同一プロセスの復帰が古い保存先設定を使う問題も修正した。
- `npm test`が成功（TypeScript、Node 45件、全internal/CLIのGoテスト、Go vet）。計画・レポート・復帰・独立レビューの対象テストはrace detectorでも成功。macOS arm64・Windows amd64のGUI・CLI・同梱rgをビルドした。実LLM・アプリ起動・画面操作・Windows実機確認は行っていない。

## 実行ごとのターン上限と再試行

ターンを累計で制限していた仕様は廃止しました。以下は実行ごとに0から数える現在の仕様の検証です。

- 過去2試行で11＋11ターンを使っていても、上限20を変えずに再試行へ追加・実行できることを確認。新しい実行ではターン・時間・ツール・読込量・検証・レビューの使用数を0から数え、同じ実行内の自動再試行では基準値を保持する。外側の試行回数上限も実行ごとに判定する。
- 過去の使用量と計画・候補・既読ルール・試行IDは保持。台帳とチェックポイントの使用量を照合して大きい方を基準にし、累計を二重加算しない。料金上限と使用量不明の制約は保持する。`maxTurns`だけの変更でも計画を保持し、他の照合対象が変わった場合は再計画する。
- ローカル模擬HTTPで、過去22ターンから今回の5ターン（編集4・独立レビュー1）で検証・レビュー合格後だけ採用し、履歴は27ターンになることを確認。今回の上限が4なら独立レビューを省略せず、候補を採用しない。送信と検証の直前に累計ターン・時間を記録する処理も維持する。
- 各編集要求・ログ・上限エラーは今回の使用数を表示し、再実行で0になることを案内する。過去の使用数だけで再実行を拒否したり、上限の引き上げや新しいキュー作成を要求したりしない。
- `read_rules`の既知ID一括取得、重複・未知IDの事前拒否、UTF-8・JSON出力サイズ制限、失敗時のキャッシュ部分反映防止を検証。ローカル模擬HTTPでは、個別10ルールの一括読込から編集・独立レビューまで5要求で完了するケースを確認。実LLMの所要ターン数や成功を保証するテストではない。
- `npm test`が成功（TypeScript、Node 38件、全internal/CLIのGoテスト、Go vet）。agent・engineの実行枠・再実行・独立レビューの対象テストもrace detectorで成功。
- macOS arm64とWindows amd64のGUI・CLI・同梱rgをビルド。実LLMへの送信、アプリ起動、画面操作、Windows実機確認は行っていない。個人の設定・実行履歴も変更していない。

## 別コンテキストの独立レビュー

- OpenAI・Azure・Claudeのローカル模擬HTTPで、選択済み接続・モデルを使い、新しい1要求の会話へ修正前・候補・必須ルール全文を送ることを確認。編集会話、計画・説明、機械検証結果、以前のレビュー指摘やツール権限を引き継がない。
- 実際のagent・検証worktree・コミット処理を通す統合テストで、機械検査NGではレビューを呼ばず、意味的なレビュー指摘は同じ編集会話へ返し、修正候補の再検証・別会話のレビュー合格後だけ採用することを確認。1試行で候補検証3回・独立レビュー2回・編集とレビュー計9要求を記録し、採用時に検証コマンドを重複実行しない。
- 候補に一致しないレビュー合格、必須ルールの評価漏れ、不正な判定・コード引用、保存できないレビューを採用しないことを確認。初回の変更不要判断は維持し、指摘後に計画を取り下げて`skipped`へ切り替える回避を拒否する。
- レビュー送信前の要求・ターン予約、応答後の使用量保存、共有上限を確認。通信結果不明のレビューはコミット・自動再送を行わず、費用上限がある場合は明示的な再実行でも追加送信を拒否する。
- レビュー中の停止後、Serviceを閉じて再作成し、明示的な再実行で保存済み候補のレビューだけを再開する統合テストが成功。同じ試行ID、候補、計6要求の累計、使用量不明フラグ、エラーと合格のレビュー履歴・個別成果物を保持する。
- 結果画面のSSRテストで、過去の要修正と最終合格の両方、指摘箇所・ルール、確認中の表示、空の使用量・配列、コード断片のHTMLエスケープを確認。TypeScript型検査も成功。ブラウザーやネイティブ画面の操作は行っていない。
- `npm test`が成功。TypeScript、Node 37件、全internal/CLIのGoテスト、Go vetを実施。`go test -race ./internal/agent ./internal/engine -run 'TestIndependent|TestReview|TestRepairCheckpoint' -count=1`も成功。
- `npm run build`と`npm run build -- --platform windows/amd64`が成功。macOS arm64・Windows amd64のGUI・CLI・同梱rgを更新。実LLMへの送信、アプリ起動、Windows実機確認は行っていない。

## 計画登録・候補検証・中断からの復帰

この節は計画・復帰機能を追加した時点の検証記録です。現在は上記の独立レビューも採用条件に含め、処理上限は実行ごとに判定します。

- `update_state`で共通・候補ルール全件の判断と箇所別計画を登録。未読ルール、不完全な判断、古いRevision、項目IDの付け替え、保留中の候補を拒否するテストが成功。
- ローカルHTTPモックで、読込→計画→不合格候補→修正候補→最終回答を1試行内で実行。旧シンボルの行番号付き指摘が返り、合格した候補の内容だけをコミットすることを確認。Azure・OpenAI・Claudeのツール履歴もモックで検証。
- 候補検証の成功・失敗・キャンセル時の巻き戻し、BOM・CRLF保持、元コードの変更検知、検証コマンドによる対象・別ファイル変更、未知のコミットの保護、最終採用時の検証コマンド重複防止を確認。
- 送信前の状態・ターン・時間予約、保存失敗時の送信抑止、使用量不明時の自動再送禁止を確認。長い検証中も時間を予約し、新候補の検証開始後は以前の合格候補へ戻れないことを検査。
- Serviceを閉じて再作成した後に、同じ試行ID・計画・既読ルール・使用量・検証回数から復帰するテストが成功。設定変更時は新しい履歴を作り、過去の成果物と通算の使用記録を保持する。次の試行の状態ファイル登録前に中断する境界も検査。
- 大量の検証出力は、完全な検証成果物を残しつつ会話・復帰状態向けに制限。失敗診断が一般的なサイズ超過エラーに置き換わらないことを確認。
- `npm test`（TypeScript、Node 33成功・1件プラットフォーム依存のスキップ、全internal/CLIのGoテスト、Go vet）が成功。agent全体とengineの修復・回復テストはrace detectorでも成功。
- macOS arm64とWindows amd64のGUI・CLI・同梱rgをビルド。アプリ起動、画面操作、実LLMへの送信、Windows実機確認は行っていない。

## 変更のない結果と全ルールの複合デモ

- 結果一覧から開く詳細は、差分がなければ「変更前」、差分があれば「差分」を初期選択する。ファイル・試行を選び直すと初期値へ戻し、同じファイルの自動更新中は手動選択を維持する。状態名だけで修正案の差分を隠さない。型検査とコードレビューで確認し、画面操作は行っていない。
- `src/workflows/dispatch-cycle.js`を追加。460行で配送計画・在庫・請求・手数料・ラベル保存・帳票・画面購読を扱い、共通3件・個別10件すべてに修正箇所を持つ。デモは28ファイル・23対象。デモ設定のみ20ターン・出力16,384トークンへ調整。
- 適用パターンがimport以外の本文にも一致すること、共通3ルールの実差分、参照解答の旧参照ゼロ、対象全文をルールへ含めないことを検査。元コードの実行を含む大型ケース10件のテストも成功。入力固定、二重課金防止、解放、保存・通知順序、全件検証、例外時の補償を含む。
- 完成解答は開発用`testdata/`に置き、生成プロジェクトとルールには含めない。実際のLLMによる全13ルールの適用成功は測定していない。
- CLIのデモ生成・抽出でも、大型ファイルに個別10件が対応付けられ、共通3件が全対象に適用されることを確認。`npm test`（Node34件、全internal/CLIのGoテスト、Go vet）が成功。
- macOS arm64・Windows amd64向けの`npm run build`が成功。GUI・CLI・rg同梱を更新。アプリの画面操作・実LLM呼び出し・Windows実機起動は行っていない。

## Gitの準備画面とデモの命名

- Gitの未導入・起動不能・利用可能を区別し、バージョン確認の時間・出力上限、秘密情報の環境変数除外、macOSのCommand Line Tools未導入時の起動抑止をテスト。対象フォルダの診断とデモ作成にも同じ確認を適用し、Gitを利用できないデモ作成ではファイルを作らないことを確認。
- デスクトップ起動時の専用ダイアログ、公式案内・再確認・後で、フッターからの再表示、再確認成功後の診断更新を実装。ブラウザープレビューでの除外と他ダイアログとの重複回避はコード上で確認。画面操作は実施していない。
- デモをParcel KitからParcel ClientへのSDK更新に統一。`Legacy*`などの名前を対象・SDK・ルール・適用パターン・参照解答・画面プレビューから除去。27ファイル・22対象・共通3件・個別10件を維持。
- 全文一致を禁止する既存テスト、構文・適用パターン・参照実装の振る舞いの検証を通過。新旧SDKの24モジュールのimportと、26ルール例のnamed importの整合性も確認。実際のLLMの精度・成功率は測定していない。
- `npm test`が成功。Nodeテスト34件、全internal/CLIのGoテストとGo vetが成功。
- `npm run build`と`npm run build -- --platform windows/amd64`が成功。macOS arm64・Windows amd64のGUI、CLI、rg同梱を更新。Windowsはクロスビルドで、実機起動は未確認。

改訂したデモは新規作成時に使用します。既存のデモやワークスペースのルールは変更しません。

## 確認・結果画面の調整

- タブ・前へ・次へ・通常の遷移処理の有効条件を共有。結果確認から、処理可能なファイルのない確認画面へフッター経由で戻れないよう修正。キューや接続の再読込後にも確認画面の条件を再検証する。
- 確認・実行設定の一覧は専用テーブルに分離。行高を34px程度に抑え、左右の余白と1行の候補ルール表示を確保。旧テーブルの大きな行高や狭い画面での列非表示を引き継がない。
- 結果一覧と詳細の境界に可変幅のグリップを追加。短いファイル名への自動フィット、長いパスでも右詳細を優先する初期幅、幅の上下限・画面縮小時の制約をテスト。ドラッグ、左右キー・Home/End、Enter・ダブルクリックによる自動幅復帰を実装。
- 詳細の独立した適用ルール行を削除し、ファイル名横へIDチップを移動。選択中の試行の説明をファイル名直下・タブ外へ常時表示し、過去試行の空の説明に最新の説明を流用しない。
- `npm test`が成功。Nodeテスト34件、全internal/CLIのGoテストとGo vetが成功。画面操作・実際のLLM呼び出しは実施していない。
- macOS arm64とWindows amd64向けの`npm run build`が成功。TypeScript、Vite、Wails GUI・CLI・rg同梱を確認。Windowsはクロスビルドで、実機起動は未確認。

## デモルールの抽象化

- 共通3件・個別10件を、API契約と判断基準、対象とは異なる用途の短い例へ変更。全22対象ファイルに対して、空白を除いても全文一致する例がないこと、対象固有の公開関数名を例に含めないことを確認。
- 個別10件の適用パターンが、中心ケース・複合ケース・判断保留ケースを引き続き抽出できることを確認。すべての対象ソースと変更前後の例のJavaScript構文をNodeで検査。
- 完成実装の参照データを開発用の`testdata/reference-solutions.json`へ分離し、生成プロジェクト・ルール資材・アプリの埋め込み対象に含まれないことを確認。所有権、保存順序、再試行、補償処理などの参照実装テストは維持。
- `npm test`が成功。Nodeテスト25件、全internal/CLIのGoテスト、Go vetを実行。実際のLLM生成結果の品質や成功率は測定していない。
- `npm run build`と`npm run build -- --platform windows/amd64`が成功。両OSのGUI・CLIに改訂したプリセットを反映。画面操作とWindows実機起動は未確認。

改訂したプリセットは新規デモ作成時に使用します。既存ワークスペースへ取り込んだルールは自動更新しません。

## v0.8.1の追加確認

- 対象抽出・チェック選択を実行設定へ移動。確認画面は選択済みファイルと候補ルールの閲覧・開始だけにし、確認への移動時には再抽出しない。
- ワークスペース、対象、保存先、絞り込み条件、サイズ上限、ルール定義の変更で抽出の有効性を失わせる。LLM接続や使用量の変化では選択を失わない。確認・開始前は保存済み設定とキューを再読込して整合性を確認する。
- 保存先変更前に未保存の選択を旧キューへ反映。プレビューの選択もメモリ上に保持し、確認表示やワークスペース切替で元に戻らないことをテスト。
- 結果画面を進捗・状態別の絞り込み・1行のファイル一覧・クリックして開く詳細に整理。適用済みルールと未採用の試行ルールを区別し、差分、前後の行番号、検証、履歴、再試行を維持。料金・トークン・コミット等は展開して確認する。
- 進捗は変更済み・変更不要・要確認を区別して集計。実行中の再試行、途中停止、開始準備のエラー、実行前の要確認、除外履歴をテスト。検索はファイル名と実際に採用されたルールID・名称を対象にする。
- `npm test`が成功。Nodeテスト25件、全internal/CLIのGoテスト（engine 39.068秒）とGo vetが成功。
- `npm run build`と`npm run build -- --platform windows/amd64`が成功。両OSのGUI・CLIとrg同梱を更新。

画面操作と実際のLLM呼び出しは実施していません。Windows版はmacOSからのクロスビルドで、実機起動は未確認です。

## v0.8.0の追加確認

- 対象フォルダ → ルール → 実行設定 → 確認 → 結果確認へ整理。確認への移動時に準備条件を検証して抽出し、ファイル選択をJSONLの`excluded`へ保存する。実行と進捗は選択済みファイルを対象とし、除外した履歴は結果一覧のフィルターで閲覧できる。
- 選択済みの1ファイルだけがAIの提案処理に渡ること、全件除外の開始拒否、再読込・再抽出の選択と完了履歴の維持、新規ファイルの初期選択、未知パス・ロック・実行中・保存失敗時の状態維持を回帰テストで確認。
- ルールの複数下書きを`SaveRules`で一括採用。リビジョン競合、重複ID、他者ロック、無効なルール、保存失敗で一部だけ採用しないこと、個人LLM接続と対象・キュー・補助資材を維持することを確認。
- デモ作成時に共通3件・個別10件を自動設定する処理と、ワークスペース公開失敗時の巻戻しを確認。プリセットは27ファイル・対象22ファイルで、階層を持つ10種類の構造・実行順序・所有権・例外処理の変更を含む。
- 同梱の変更後コード10例をローカルAPIで検証し、flush・ackの順序、部分初期化後の解放、冪等性、事前検証と副作用の分離などを確認。このテストはAIの生成結果を検証する実行ゲートではない。
- `npm test -- --race`が成功。Nodeテスト15件、全internal/CLIのGoテスト（engine 47.123秒、catalog 18.036秒）とGo vetを実行。CLIの古いfixtureも初回コミット済みのGit条件へ更新。
- `npm run build`と`npm run build -- --platform windows/amd64`が成功。TypeScript型検査、Vite、Wailsの生成バインディングとGUI、rg・原文ライセンスの同梱、単独CLIを含む。
- 共通の開発コマンドについて、対象OSとrg設定の引き継ぎ、GUI→CLIの順序、ビルド失敗時の停止、Mac梱包時の既定のアドホック署名をNodeテストで確認。

今回のUI変更はコード・型検査・ビルドまでを確認し、画面操作は実施していません。実際のLLMサービスは呼んでいません。Windows版はmacOSからのクロスビルドで、Windows実機起動は未確認です。

## v0.7.0の追加確認

- ワークスペースごとのエラーアイコン、ホバーで開く吹き出し、別ワークスペースの指定ルール明細への移動、明細タイトルのアイコンと「エラー詳細」を確認。読み込めないルールもIDで選択可能。
- 1024×576のプレビューと1143×768のMacウィンドウで、一覧・本文の最下部、編集時の「ルールを保存」が固定フッターより上に表示されることを確認。
- 削除確認はキャンセルへ初期フォーカス。Escapeで取り消し、確認後のルール・ワークスペース削除と一覧更新、エラー解消時のアイコン消去を確認。未保存のルール変更がある間は両方の削除を無効化。
- Mac専用ウィンドウで検証用の旧形式R001を削除し、R019・R020が復旧することを確認。続けてワークスペースを削除し、元ルール版と設定がローカル退避先に残り、外部キューが残ること、対象ソースの全ファイルのハッシュが操作前後で一致することを検査。
- 起動時の非選択ワークスペースの診断、切替後のエラー保持、修復後の消去、実行中の再検証回避、失敗ファイルへの遷移情報と秘密値除去、他者ロック・実行中・古い選択からの削除拒否、不正ルールの個別削除と最後の削除後の新規作成を回帰テストで確認。

`go test -race ./internal/... ./cmd/...`（engine 28.913秒、catalog 18.242秒）、`go vet ./...`、TypeScript型検査・本番ビルド、差分行番号の7件のテストが成功。実際のLLMサービスは呼んでいません。Windows GUIの実機動作は未確認です。

## 結果差分の行番号

- 差分の行番号を変更前・変更後の2列にし、Gitのハンク情報から実際のソース行番号を表示。共通行の「5 / 5」、削除行の変更前番号、追加行の変更後番号をプレビューの実画面で確認。
- 追加・削除で前後の番号がずれる場合、複数ハンク・複数ファイル、件数省略・0件、新規・削除ファイル、ヘッダーに似たソース行、最終改行なし、CRLFを7件の回帰テストで確認。`node --test scripts/diff_line_numbers.test.mjs`が成功し、CIにも追加。
- TypeScriptの型検査と本番ビルドが成功。処理結果やソースファイルの保存内容は変更せず、表示で行番号を解釈する。

## v0.6.0の追加確認

- ルール定義を`rules/<ID>/rule.json`へ統一し、名称・変更概要・変更前・変更後・備考・保留条件・1行パターンを独立したプロパティとして保存。パッケージはversion 2に変更し、旧形式は既存ファイルを変更せず読み込みエラーにする。
- デスクトップ画面で5項目へ別々の値を入力し、変更前後の左右配置と中央の矢印、詳細内のスクロール、最下部の1行パターンを確認。保存後のJSONのキーと値・改行が完全一致し、rule.md等が作られないことを検査。
- CSVの8列を各項目へ直接保存。個別上書き・未選択保持・新規マージ、各項目を含むrevision、編集ロック、全件巻戻しの回帰を維持。
- AIが受け取る共通ルールと`read_rule`の本文には、同じ変換処理で5項目のMarkdownを生成。実行経路の統合テストで、順序どおりの見出しとコードを渡し、保存JSONや適用パターンを本文へ混ぜないことを確認。
- 厳密なJSON検証、改行パターン拒否、全項目の保存復元・競合検出、コードフェンス内のバッククォート保持、個別ルール索引の長さ制限、旧形式拒否と新パッケージへの入替をテスト。

`go test -race ./internal/... ./cmd/...`（engine 25.532秒、catalog 17.436秒）、`go vet ./...`、TypeScript型検査・本番ビルドが成功。macOS arm64とWindows amd64のGUI・CLIをビルドしました。実画面確認は使い捨て設定で実施し、実際のLLM APIは呼んでいません。Windows GUIの実機動作は未確認です。

## v0.5.2の追加確認

- 通常のWailsビルドでもrgとライセンスを同梱するフックを追加。macOSとWindowsのビルドでフックの実行と同梱先を確認し、macOSアプリの署名を検査。
- PATHにrgがない状態で、同梱rgを使用したサンプルのルール読込・対象抽出に成功。別プロセスを使うresolver回帰と、Wailsが返すアプリ内実行ファイルパスからの同梱・署名を含むスクリプト4テストも成功。
- 既存ワークスペースの探索・追加APIと画面を削除。「＋」から直接新規作成する画面を確認。保存済みワークスペースはローカル一覧で選択。
- 新規本文の5見出しと独立した名称欄を実画面で確認し、保存後のrule.mdが指定テンプレートに完全一致することを検査。
- CSVファイルを実画面から選び、2つの重複IDのうち1つだけを上書き。結果の「1件追加・1件上書き・1件保持」、名称、変更しなかった本文・パターン、引用符・カンマ・改行を含むセルの保存内容を確認。
- CSV変更、名前を含むrevisionの変更、新規IDの後発衝突、ワークスペース切替、他者の編集ロック、無効な正規表現の全件巻戻し、未選択ルール・補助資材の保持、全保持時の変更なしを回帰テストで確認。

`go test -race ./internal/... ./cmd/...`（engine 26.378秒、catalog 17.544秒）、TypeScriptの型検査・本番ビルド、`go vet ./...`が成功。macOS arm64とWindows amd64のGUI・CLIをビルドしました。ネイティブ画面の確認は使い捨てのローカル設定で実施し、実際のLLM APIは呼んでいません。Windows GUIの実機動作は未確認です。

## v0.5.1の追加確認

読み込めない形式・破損・復号不能なLLM接続設定は、その接続設定ファイルだけをロック下で破棄して未設定へ戻します。暗号化キー、ワークスペース、無関係な設定は保持します。権限・I/Oエラーでは削除しません。

不正な8形式、破棄後の再登録と再起動、同時読込・書込、保存済みの正常ファイル保持、対象inodeの置換、権限エラーとリンクを使った回帰テストがrace付きで成功しました。

## v0.5.0の追加確認

保存先をアプリ内の`workspaces/<ID>/setting.json`へ統一し、対象フォルダ内への設定作成と旧形式の互換処理を廃止しました。LLM接続IDも各ワークスペースに保存し、接続定義・認証情報は個人用の暗号化保存先に分離します。

- 共通ルールがある場合、旧シンボルの一致に関係なくglob・固定除外後の全ファイルを抽出。共通ルールなしの絞り込みと修正後の残存検査は維持。
- `.oborules`の読込・書出、設定と補助資材の保持、ZIPと正規表現の検証、失敗時の巻き戻し、元パッケージと対象ソースの保持。
- ルール追加・編集、別プロセスの編集者表示と書込拒否、画面離脱・終了時のロック解放、古い下書きのrevision不一致による上書き拒否。
- パッケージ変更後の再抽出要求、中断後のパッケージと実行履歴の復元、パイロットからの再開。
- LLM接続IDのローカル設定への保存、一般設定APIによる選択変更の拒否、閲覧専用側への更新反映、キュー・レポートへの個人設定の混入防止。
- ブラウザーで1行のルール一覧、詳細ヘッダー、適用パターン、Markdownの見出しとコード表示、詳細下端までの独立スクロール、閉じた詳細設定、rg入力の撤去を確認。
- macOSの確認用ウィンドウでルールの新規追加・保存と表示への切替、標準タイトルバーの表示を確認。ウィンドウ移動の実操作は未検証。

`go test -race ./internal/... ./cmd/...`、`go vet ./...`、TypeScript型検査と本番ビルドが成功しました。Goのテストはローカル模擬HTTP APIと一時Gitリポジトリを使い、実際のLLMサービスには送信していません。macOS arm64とWindows amd64のGUI・CLIをビルドしました。Windows GUIの実機動作は未確認です。

以下の旧版の記録は当時の検証内容です。v0.5.0の保存形式と互換性を意味しません。

## v0.4.0の追加確認

LLM接続を端末内の名前付き接続として独立させ、左メニューから登録・編集・削除できるようにしました。ワークスペースの実行画面で接続を選択し、対象フォルダ画面は表示だけに、絞り込み・ripgrep・キューと結果の保存先はルール画面にまとめています。

- 接続登録だけをワークスペースなしで利用できること。複数ワークスペースで同じ接続を選択し、再起動後にも個人の選択を復元すること。
- ワークスペース設定の保存が登録済みの接続を上書きしないこと。共有設定・キュー・レポートへ接続一覧や個人の選択を含めないこと。
- v0.3以前の個人接続を暗号化された共通一覧へ移し、削除した接続が旧データから復活しないこと。
- 閲覧専用ワークスペースを開いた状態でも、個人の接続を管理できること。別アプリからの追加・変更を失わず保存すること。
- 接続テスト中も終了時のキャンセルが効くこと。CLIの接続上書きフラグは別接続を作成し、既存の再利用する接続を変更しないこと。
- 同時初回起動時の暗号鍵初期化と、複数プロセスによる暗号化接続一覧の更新。
- ブラウザでサイドバーの区分、独立した接続追加・プロバイダー切替、実行ボタン左の接続選択、5タブの構成、設定保存先の帯の削除、LLMプロバイダーを先頭にした一列の設定フォームを確認。

エンジン・CLI・暗号化保存のraceテスト、`go vet ./...`、TypeScript型検査と本番ビルドは合格しています。接続テストにはローカルの模擬HTTP APIだけを使用し、実際のLLM APIへは送信していません。

## v0.3.0の追加確認

個人LLM設定・フォルダ指定から始めるワークスペース作成・明示的な既存選択・編集ロックを反映しました。`go test -race ./internal/... ./cmd/...`、`go vet ./...`、TypeScript型検査と本番ビルドに合格しています。

- 初回の乱数キー生成、暗号化保存、再起動復元、ユーザー・ワークスペース間の分離、接続先変更時の資格情報破棄、保存済み資格情報の削除。
- 共有設定・セッション・レポートに個人LLM設定を含めないこと。旧形式は登録済みユーザーの設定のみ移行し、新規ユーザーは共有の旧接続を使用しないこと。
- 対象フォルダの未指定を拒否し、作成後の変更と別フォルダのJSONL取込を拒否すること。既存候補の検索は登録・設定変更を行わず、選択したワークスペースだけを開くこと。
- 別サービス・別プロセスでの編集者表示、閲覧専用側の書込拒否、切替・終了・プロセス異常終了後の解放、再選択による編集権限の取得。
- 追加のraceテストで、終了時のキャンセル後処理が完了するまで編集ロックを保持することと、共有設定を開くだけでは指定された独自のrg実行ファイルを起動しないことを確認。
- 共有出力の新規アクセス権、既存の非公開設定の維持、私有ソースの差分公開範囲、不正な出力先の拒否。
- プレビューで「＋」メニュー、フォルダ指定後の作成、既存候補の選択、閲覧専用表示、対象フォルダの固定、ヘッダーの重複ボタンとブランド副題の削除を確認。

実際のLLM APIには送信していません。別OSユーザーの実機・SMBサーバー上のロックやACL、Windows GUI・DPAPIの実機動作は未確認です。クラウド同期で独立したローカルコピー間の排他は保証しません。

## v0.2.1の追加確認

補足説明を項目名右側の「？」から開く吹き出しに集約しました。TypeScript型検査・本番ビルドに合格し、ブラウザで6タブの表示、クリックによる固定・再クリック・外側クリック・Escによる終了、キーボードフォーカス、吹き出しの単一表示を確認しました。2行のAzure見出しとアイコンの垂直中心が一致することも確認しています。エラー・未保存・検証結果の表示は維持しています。

変更は表示部分のみです。今回、本番LLM APIへの送信や移行処理の実行は行っていません。

## v0.2.0の追加確認

ワークスペース管理・6タブUI・LLMプロバイダー切替を反映した後、全Go raceテスト、`go vet ./...`、TypeScript型検査・本番ビルドはすべて合格しました。

- 同じ対象フォルダを使うワークスペースの作成・複製・再起動後の復元、固定IDと設定・キュー・履歴の分離。
- `OneByOne/workspaces/<ID>/` の分割JSON保存、旧形式の読込、APIキーをファイルや別ワークスペースへ引き継がないこと。
- `OneByOne/`配下の抽出・手動JSONL・周辺コード読取からの排除。設定だけのGit未コミット変更は許容し、元ソースの変更は拒否。
- 設定が不完全なワークスペースも開いて修復できること。選択に失敗した場合、表示対象と内部の操作対象が食い違わないこと。
- OpenAI・Azure・Claudeそれぞれの認証ヘッダー、接続確認、ルール／周辺コードのツール呼出し、構造化修正案、使用量、上限、拒否・不完全応答・通信失敗をローカル模擬APIで検証。
- 検証コマンドの子プロセスへ、3プロバイダーのAPIキーとAzureトークンが渡らないこと。
- ブラウザ上で6タブの順送り、ワークスペース切替、同一フォルダの別ID表示、3種類のLLM設定画面、候補一覧、差分・検証結果を確認。

この追加確認で本番のLLM APIへのリクエストは送信していません。

## 実施済み

- `go test -race ./internal/... ./cmd/...`: 全パッケージ合格。rgの実ファイル照合、模擬HTTP API、使い捨てGitリポジトリを使用。
- `go vet ./...`: 合格。
- `npm --prefix frontend run build`: TypeScriptの型検査と本番ビルドに合格。
- macOS arm64のGUI・CLI、およびWindows amd64のGUI・CLIをビルド。
- macOSの専用ウィンドウを起動し、対象一覧・ルール・検証結果・試行履歴を表示できることを確認。
- ブラウザの明示的なプレビューで、選択・絞り込み・ルール表示・設定・ログ表示を確認。

回帰テストには、1ファイル1コミット、元ソースの保持、検証失敗時の巻き戻し、再試行上限、中断前後の台帳とコミットの照合、BOM / CRLF、範囲外編集の拒否、複数プロセスのロック、再抽出後の履歴保持、設定読込時の認証情報の分離、料金未確認時の再送停止を含みます。

## 配布物と確認範囲

macOS版はApple Silicon向けです。Windows版はこのmacOS上でクロスビルドしたもので、PEヘッダーによるOS・CPU・GUI / CLIの種別を確認しています。Windows実機でのGUI起動、WebView2導入、フォルダ選択などの動作は未確認です。各OSでビルド・テストするGitHub Actionsの定義も含めていますが、この作業ではActions自体を実行していません。

同梱するrgは公式15.2.0アーカイブのSHA256を照合しています。配布スクリプトはOS・CPU・ライセンスファイル・大文字小文字を無視したパス衝突を検査し、macOSでは同梱後のアドホック署名も検証します。

Azureに関する自動テストは模擬APIによるものです。使用する実デプロイの認証・利用可能なモデル機能・クォータは、利用者の設定で確認してください。ビルド・テストによる移行結果の検証には、対象プロジェクト自身の検証コマンドが必要です。

開発者証明書を使った配布署名とmacOS公証は未実施です。
